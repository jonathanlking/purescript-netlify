{
  "name": "purescript-netlify",
  "private": true,
  "scripts": {
    "prebuild" : "./install-deps.sh"
    "build": "./node_modules/.bin/pulp browserify --to dist/app.js && stack help",
    "postinstall": "./node_modules/.bin/bower cache clean && ./node_modules/.bin/bower install"
  },
  "dependencies": {},
  "devDependencies": {
    "bower": "^1.8.2",
    "pulp": "^12.0.0",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.6.0",
    "psc-package": "^0.2.5"
  }
}
